# 系统架构图

> 本文档用 Mermaid 描述 `stability_harness_loop_multiagent` 的架构,
> 对应 `AGENTS.md` 与三引擎设计。三引擎在**模块层**强制互不 import,
> 所有跨引擎通信只走 `EventBus`(pub/sub + request/reply + `#` 通配)。

## 1. 5 层契约内核架构总览

自上而下 5 层:`core/` 契约内核 + 三引擎(`harness/` `loop/` `multi_agent/`) + `business/` 装配层。

```mermaid
flowchart TB
    subgraph L1["L1 · core/ 契约内核(零内部依赖)"]
        C1["EventBus<br/>(唯一接缝)"]
        C2["Agent 基类<br/>+ AgentSpec"]
        C3["combine_votes<br/>(投票合并规范)"]
    end
    subgraph L2["L2 · harness/ 治理引擎(让系统活着且受约束)"]
        H1["Runtime<br/>Agent 生命周期+监督器"]
        H2["Watchdog<br/>存活/死锁检测"]
        H3["Telemetry<br/>trace/metric/log/Sink"]
        H4["EngineBusTracer<br/>引擎归属归一化"]
        H5["Governance<br/>鉴权/配额/熔断/预算"]
        H6["Verifier<br/>护栏/校验/评估钩子"]
    end
    subgraph L3["L3 · loop/ 控制引擎(确定性迭代+事实独裁)"]
        L1A["ControlLoop<br/>(sense→decide→halt)"]
        L2A["DecisionAuthority<br/>事实独裁裁决"]
        L3A["SharedContext<br/>循环独占写入"]
        L4A["TerminationPolicy<br/>OR 组合中止条件"]
        L5A["Scheduler<br/>自适应间隔/退避"]
    end
    subgraph L4["L4 · multi_agent/ 多智能体引擎(执行+建议+观察)"]
        M1["WorkerAgent<br/>执行型"]
        M2["AdvisorAgent<br/>投票/事件"]
        M3["ObserverAgent<br/>记录/面板"]
        M4["TargetAdapter<br/>目标适配协议"]
    end
    subgraph L5["L5 · business/ 领域装配层(可 import 任意引擎,不被反向依赖)"]
        B1["Hikvision 门禁<br/>adapter/advisor/client/llm"]
        B2["Scenario YAML 场景<br/>schema/adapter/worker/runner"]
        B3["capabilities/<br/>actions/preconditions/probes"]
    end

    %% 跨层:EventBus 扇出到三引擎
    C1 -->|publish/subscribe| H1
    C1 -->|loop/tick| L1A
    C1 -->|loop/tick| M1

    %% L3 内部(Loop 控制链)
    L1A -->|裁决| L2A
    L1A -->|独占写入| L3A
    L1A -->|退避| L5A
    L1A -->|中止判断| L4A

    %% L4 内部(MAS 执行链)
    M1 -->|act 操作| M4

    %% L4 → L3 回流(投票/事件上报)
    M2 -->|agent/vote/reply| L2A
    M3 -->|loop/done| L1A

    %% L2 → L4 治理闸门(fail-closed,只拦操作不 halt 循环)
    H5 -.fail-closed 闸门.-> M1
    H6 -.护栏校验.-> M1

    %% L2 → L3 可观测
    H3 -.trace/metric.-> L1A
    H4 -.全总线追踪.-> C1

    %% L5 装配(business 可 import 任意引擎)
    B1 -.装配 adapter.-> M4
    B1 -.装配 advisor.-> M2
    B2 -.装配 scenario_adapter.-> M4
    B2 -.装配 scenario_worker.-> M1
    B3 -.被 scenario_worker 装配.-> B2
```

**5 层职责对照**:

| 层 | 包路径 | 模块数 | 职责 |
|----|--------|--------|------|
| L1 core/ | `stability_harness_loop_multiagent/core/` | 3 | 契约内核(零内部依赖,三引擎共同依赖) |
| L2 harness/ | `stability_harness_loop_multiagent/harness/` | 6 | 治理引擎(让系统活着且受约束) |
| L3 loop/ | `stability_harness_loop_multiagent/loop/` | 5 | 控制引擎(确定性迭代 + 事实独裁) |
| L4 multi_agent/ | `stability_harness_loop_multiagent/multi_agent/` | 4 | 多智能体引擎(执行 + 建议 + 观察) |
| L5 business/ | `stability_harness_loop_multiagent/business/hikvision/` | 3 | 领域装配层(Hikvision + Scenario + capabilities) |

## 2. 三引擎总览(简化版)

`core(EventBus)` 是三引擎共同依赖的契约内核;`harness ←(EventBus)→ loop ←(EventBus)→ multi_agent`,边界在模块层强制。

```mermaid
flowchart LR
    subgraph C[Core 契约内核]
        C1[EventBus 唯一接缝]
        C2[Agent 基类]
        C3[combine_votes]
    end
    subgraph H[Harness 引擎]
        H1[Runtime 运行时/监督]
        H3[Watchdog 存活/死锁]
        H4[Telemetry 可观测]
        H5[Governance 鉴权/熔断/预算]
        H6[Verifier 护栏/校验]
    end
    subgraph L[Loop 引擎]
        L1[ControlLoop 控制循环]
        L2[DecisionAuthority 事实独裁]
        L3[SharedContext 不可变上下文]
        L4[TerminationPolicy 中止条件]
        L5[Scheduler 调度/退避]
    end
    subgraph M[MAS 多智能体]
        M1[Worker 执行]
        M2[Advisor 投票/事件]
        M3[Observer 记录/面板]
        M4[TargetAdapter 目标适配]
    end
    C1 <-->|EventBus| H1
    C1 <-->|EventBus| L1
    C1 <-->|EventBus| M1
    L1 <-->|EventBus| M2
    L1 <-->|EventBus| M3
    M1 -->|操作| M4
    H5 -.治理闸门.-> M1
    H6 -.校验.-> M1
```

## 3. 控制循环(ControlLoop)

`sense → plan → act → check → decide → halt` 的确定性循环,每轮必产生 `Verdict`,
且受 `max_rounds` / `max_duration` 硬上限保证一定终止。

```mermaid
flowchart TD
    Start([开始]) --> Tick{loop/tick}
    Tick --> Sense[感知: 收集 target/checked + target/recovered]
    Sense --> Plan[计划: 生成本轮操作]
    Plan --> Act[执行: Worker 经 TargetAdapter 操作目标]
    Act --> Check[探测: probe 产出事实]
    Check --> Decide{DecisionAuthority.decide}
    Decide -->|事实全 True| Pass[Verdict: pass]
    Decide -->|任一事实 False| Fail[Verdict: fail]
    Decide -->|error / 不确定| Warn[Verdict: warn 60 保守]
    Decide -->|critical 事件| Recheck[recheck x1]
    Pass --> Halt{TerminationPolicy}
    Fail --> Halt
    Warn --> Halt
    Recheck --> Sense
    Halt -->|未满足| Tick
    Halt -->|max_rounds/duration 或 abort| Done([loop/done 退出])
```

## 4. 投票与事实独裁

Advisor 只投 `(risk, confidence)`，不裁决；`DecisionAuthority` 拥有唯一裁决权。
任一事实为 `False` → 本轮 `fail`，不可被风险分或投票翻转为 `pass`。

```mermaid
flowchart LR
    A1[Advisor A] -->|vote risk,conf| Combine[combine_votes / 本地镜像合并]
    A2[Advisor B] -->|vote risk,conf| Combine
    A3[Advisor C] -->|vote risk,conf| Combine
    Facts[探测事实] --> Decide{DecisionAuthority}
    Combine -->|附加 warn/recheck 注解| Decide
    Decide -->|事实 False| F[FAIL 不可翻转]
    Decide -->|事实全 True| P[PASS]
    Decide -->|error| W[WARN 60 保守]
```

## 5. 数据驱动场景层(门禁对讲)

零代码新增用例：复制 YAML 模板改字段，`scenario_worker` 把探测结果产出事实交给 `DecisionAuthority`。

```mermaid
flowchart LR
    Y[cfg YAML<br/>target+stress+probe+loop] --> Runner[run_scenario]
    Runner --> Adapter[ScenarioAdapter<br/>真机/ dry-run]
    Adapter --> Worker[ScenarioWorker]
    Worker -->|act| Target[(门禁对讲设备)]
    Worker -->|probe 事实| Decide[DecisionAuthority]
    Decide --> Report[治理面板/Observer]
```

## 不变量速记

- **循环终止**：`max_rounds` / `max_duration` 硬上限，不可能死锁。
- **裁决产生**：每轮必出 `Verdict`。
- **事实独裁**：注入失败事实强制 `fail`，即使 Advisor 投低风险。
- **事件扇出**：Observer 必收 `loop/done`，总线端到端可用。
- **防死锁**：每个跨引擎 `await` 都有超时 + 确定性兜底；`recheck` 上限 1 次。
